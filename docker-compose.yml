services:
  cli-proxy-api:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VERSION: ${VERSION:-dev}
        COMMIT: ${COMMIT:-none}
        BUILD_DATE: ${BUILD_DATE:-unknown}
    environment:
      DEPLOY: cloud
      PGSTORE_DSN: "postgresql://lijiahua%40looplaygame.com:LOOPLAY2026@cpa-cpa-db-hofpqp:5432/CPADB?sslmode=disable"
      PGSTORE_SCHEMA: "public"
    ports:
      - "8318:8317"
    volumes:
      - "../files/logs:/CLIProxyAPI/logs"
    restart: unless-stopped
