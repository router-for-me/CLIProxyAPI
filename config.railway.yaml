# Railway deployment configuration for CLIProxyAPI
# This file is dynamically generated by start-cloud.sh
# Configure via Railway environment variables:
#
# Required:
#   - API_KEY: Your API key for client authentication
#   - MANAGEMENT_PASSWORD: Password for accessing management panel
#
# Optional:
#   - PORT: Server port (default: 8080)
#   - DEBUG: Enable debug logging (true/false, default: false)
#   - PROXY_URL: Upstream proxy URL (e.g., socks5://host:port)
#   - ALLOW_REMOTE_MANAGEMENT: Allow non-localhost management access (true/false, default: true)
#
# This file will be overwritten on container startup.
# Do not manually edit this file in production.

host: ""
port: 8080
api-keys:
  - "configure-via-env-API_KEY"
auth-dir: "/CLIProxyAPI/.cli-proxy-api"
