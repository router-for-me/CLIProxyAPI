# 复制为 .env 后再调整。Docker Compose 会自动加载项目根目录下的 .env。

# ---------------------------------------------------------------------------
# 配置文件选择
# ---------------------------------------------------------------------------
# 挂载到容器内的配置文件路径。
# - ./config.prod.yaml  (默认,生产配置:debug=false、Docker 路径)
# - ./config.yaml       (本地开发:debug=true、~/.cli-proxy-api)
# CLI_PROXY_CONFIG_PATH=./config.prod.yaml

# ---------------------------------------------------------------------------
# 镜像 / 构建
# ---------------------------------------------------------------------------
# 使用预构建镜像,而不是从源码构建。
# CLI_PROXY_IMAGE=eceasy/cli-proxy-api:latest

# 构建元信息(仅展示用)。
# VERSION=dev
# COMMIT=none
# BUILD_DATE=unknown

# ---------------------------------------------------------------------------
# 持久化
# ---------------------------------------------------------------------------
# 宿主机目录,挂载到容器 /var/data/cli-proxy-api(日志、auth、冷却状态)。
# CLI_PROXY_DATA_PATH=./data

# ---------------------------------------------------------------------------
# 集群模式专用(docker-compose.cluster.yml)
# ---------------------------------------------------------------------------
# 集群模式必须提供 HOME_JWT,可通过管理 API 生成。
# HOME_JWT=
# CLI_PROXY_HOME_PATH=./home
# CLI_PROXY_LOG_PATH=./logs
# CLI_PROXY_PLUGIN_PATH=./plugins

# ---------------------------------------------------------------------------
# 其他
# ---------------------------------------------------------------------------
# 部署标记(仅展示,部分日志会带出)。
# DEPLOY=
