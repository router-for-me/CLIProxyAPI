import{_ as t,o as a,c as i,ag as o}from"./chunks/framework.DM0yugQT.js";const m=JSON.parse('{"title":"Planning Quality Lifecycle","description":"","frontmatter":{},"headers":[],"relativePath":"planning/README.md","filePath":"planning/README.md","lastUpdated":1771809538000}'),c={name:"planning/README.md"};function l(d,e,n,s,u,r){return a(),i("div",null,[...e[0]||(e[0]=[o('<h1 id="planning-quality-lifecycle" tabindex="-1">Planning Quality Lifecycle <a class="header-anchor" href="#planning-quality-lifecycle" aria-label="Permalink to &quot;Planning Quality Lifecycle&quot;">​</a></h1><h2 id="quality-command-matrix" tabindex="-1">Quality Command Matrix <a class="header-anchor" href="#quality-command-matrix" aria-label="Permalink to &quot;Quality Command Matrix&quot;">​</a></h2><ul><li><code>task quality:fmt</code> — Format all Go sources in repo.</li><li><code>task quality:fmt:check</code> — Validate formatting without mutation.</li><li><code>task quality:ci</code> — Pre-merge quality gate (non-mutating; fmt check + vet + optional staticcheck + diff/staged lint).</li><li><code>task quality:fmt-staged</code> — Format and lint staged files only.</li><li><code>task quality:fmt-staged:check</code> — Check formatting and lint staged/diff files (PR-safe, non-mutating).</li><li><code>task quality:quick</code> — Fast loop (<code>QUALITY_PACKAGES</code> scoped optional), readonly.</li><li><code>task quality:quick:fix</code> — Auto-fix local loop (format all + staged format/lint + quick checks).</li><li><code>task quality:quick:check</code> — Fast non-mutating quality loop (<code>quality:fmt:check</code> + <code>lint:changed</code> + targeted tests).</li><li><code>task quality:quick:all</code> — Run <code>quality:quick</code> and equivalent sibling project quality checks via <code>quality:parent-sibling</code>.</li><li><code>task lint</code> — Run <code>golangci-lint</code> across all packages.</li><li><code>task lint:changed</code> — Run <code>golangci-lint</code> on changed/staged Go files.</li><li><code>task test:smoke</code> — Startup and control-plane smoke test subset in CI.</li><li><code>task quality:vet</code> — Run <code>go vet ./...</code>.</li><li><code>task quality:staticcheck</code> — Optional staticcheck run (<code>ENABLE_STATICCHECK=1</code>).</li><li><code>task quality:release-lint</code> — Validate release-facing config examples and docs snippets.</li><li><code>task test:unit</code> / <code>task test:integration</code> — Tag-filtered package tests.</li><li><code>task test:baseline</code> — Run <code>go test</code> with JSON and plain-text baseline output (<code>target/test-baseline.json</code> and <code>target/test-baseline.txt</code>).</li><li><code>task test</code> — Full test suite.</li><li><code>task verify:all</code> — Unified local audit entrypoint (<code>fmt:check</code>, <code>test:smoke</code>, <code>lint:changed</code>, <code>release-lint</code>, <code>vet</code>, <code>staticcheck</code>, <code>test</code>).</li><li><code>task hooks:install</code> — Install local pre-commit checks.</li></ul><h2 id="recommended-local-sequence" tabindex="-1">Recommended local sequence <a class="header-anchor" href="#recommended-local-sequence" aria-label="Permalink to &quot;Recommended local sequence&quot;">​</a></h2><ol><li><code>task quality:fmt:check</code></li><li><code>task quality:quick</code></li><li><code>task lint:changed</code></li><li><code>task quality:vet</code> (or <code>task quality:staticcheck</code> when needed)</li><li><code>task test</code> (or <code>task test:unit</code>)</li><li><code>task test:smoke</code></li><li><code>task verify:all</code> before PR handoff.</li></ol><h2 id="ci-alignment-notes" tabindex="-1">CI alignment notes <a class="header-anchor" href="#ci-alignment-notes" aria-label="Permalink to &quot;CI alignment notes&quot;">​</a></h2><ul><li><code>preflight</code> is shared by all test/quality tasks and fails fast on missing <code>go</code>, <code>task</code>, or <code>git</code>.</li><li><code>preflight</code> also validates <code>task -l</code>, and if a <code>Makefile</code> exists validates <code>make -n</code> for build-task sanity.</li><li><code>task</code> now includes <code>cache:unlock</code> in test gates to avoid stale lock contention.</li><li>CI baseline artifacts are now emitted as both JSON and text for auditability.</li></ul>',7)])])}const f=t(c,[["render",l]]);export{m as __pageData,f as default};
