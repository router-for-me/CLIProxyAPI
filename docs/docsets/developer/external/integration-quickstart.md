# Integration Quickstart

1. Start cliproxyapi++ with config and auth storage.
2. Point OpenAI-compatible clients to proxy `/v1` endpoints.
3. Validate provider model mapping and fallback behavior.
4. Add health and quota observability to your platform stack.
