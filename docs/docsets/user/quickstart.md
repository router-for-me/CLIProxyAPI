# Technical User Quickstart

1. Configure `config.yaml` from the example.
2. Start service with Docker or native binary.
3. Validate `GET /v1/models` and sample chat completions.
4. Monitor rate limits and provider-specific auth state.
