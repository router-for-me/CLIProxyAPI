# 上游 v7.2.133 干净合并实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**目标：** 将上游 `v7.2.133` 合并到二开 Fork，并完整保留日志录制、JSONL 转换、Zstandard 压缩、TOS 上传、Supabase 上报和日志大小统计。

**架构：** 以 Fork 当前 `main` 为基础直接合并上游稳定标签，采用上游拆分后的 Server 和 RequestLogger 架构，再把 Fork 的集成点迁移到上游对应职责文件。Fork 独立新增的 uploader、log QA 和管理页面模块保持原样，通过现有行为测试验证接口兼容性。

**技术栈：** Go 1.26、Gin、Zstandard、火山引擎 TOS SDK、Supabase HTTP ingest、Git merge/worktree。

---

### 任务 1：建立合并前基线

**文件：**
- 验证：`internal/logging/`
- 验证：`internal/api/middleware/`
- 验证：`internal/loguploader/`
- 验证：`internal/logqa/`
- 验证：`internal/api/handlers/management/`

- [ ] **步骤 1：下载 Go 模块依赖**

运行：`go mod download`

预期：命令退出码为 0。

- [ ] **步骤 2：运行二开日志链路专项测试**

运行：

```bash
go test ./internal/logging ./internal/api/middleware ./internal/loguploader ./internal/logqa ./internal/api/handlers/management ./internal/managementasset
```

预期：所有包通过；若本地沙箱禁止 `httptest` 监听端口，则在允许本地回环监听的环境重新执行同一命令。

- [ ] **步骤 3：运行合并前编译验证**

运行：`go build -o test-output ./cmd/server && rm test-output`

预期：编译成功且临时二进制被删除。

### 任务 2：合并上游稳定标签

**文件：**
- 合并来源：`refs/tags/v7.2.133`
- 合并目标：`codex/upgrade-v7.2.133-clean`

- [ ] **步骤 1：确认工作区干净和标签指向**

运行：

```bash
git status --short --branch
git show -s --format='%H %D%n%s' v7.2.133
```

预期：除本实施计划外无未提交文件，标签指向上游稳定提交 `7efe0a7c`。

- [ ] **步骤 2：提交实施计划**

运行：

```bash
git add -f docs/superpowers/plans/2026-08-16-upstream-v7.2.133-clean-merge.md
git commit -m "docs: plan clean upstream v7.2.133 merge"
```

预期：计划提交成功。

- [ ] **步骤 3：开始非提交式合并**

运行：`git merge --no-ff --no-commit v7.2.133`

预期：Git 停在冲突状态，冲突文件由 `git diff --name-only --diff-filter=U` 精确列出。

### 任务 3：解决非 Go 核心冲突

**文件：**
- 修改：`.gitignore`
- 修改：`docker-compose.yml`
- 修改：`go.mod`
- 生成：`go.sum`

- [ ] **步骤 1：合并忽略规则**

保留 Fork 的：

```gitignore
.gocache/
/.qoder
/split_logs
.worktrees/
```

同时保留上游的：

```gitignore
docker-compose.override.yml
.llm-wiki
.gitnexus/
```

- [ ] **步骤 2：合并 Docker 服务定义**

保留现有 `cli-proxy-api`、`log-uploader` 和 `log-qa` 服务、日志目录挂载、插件目录挂载及 TOS 环境变量。插件目录变量采用兼容表达式：

```yaml
- ${CLI_PROXY_PLUGINS_PATH:-${CLI_PROXY_PLUGIN_PATH:-./plugins}}:/CLIProxyAPI/plugins
```

保留上游对主服务的新增配置，但不得删除 Fork 的 uploader/QA 服务和持久化挂载。

- [ ] **步骤 3：合并 Go 依赖**

保留：

```go
github.com/volcengine/ve-tos-golang-sdk/v2 v2.9.7
```

采用上游的 tokenizer 与 `golang.org/x/*` 版本，并保留 Fork 仅有的直接依赖。运行：`go mod tidy`，由 Go 工具生成一致的 `go.sum`。

- [ ] **步骤 4：检查配置冲突结果**

运行：

```bash
git diff --check
docker compose config --quiet
```

预期：无冲突标记、无空白错误，Compose 配置可解析。

### 任务 4：迁移 API Server 二开集成

**文件：**
- 修改：`internal/api/server.go`
- 修改：`internal/api/server_management.go`
- 修改：`internal/api/server_middleware.go`
- 修改：`internal/api/server_routes.go`
- 测试：`internal/api/server_test.go`
- 测试：`internal/api/server_request_logger_policy_test.go`
- 测试：`internal/api/handlers/management/request_log_usage_test.go`

- [ ] **步骤 1：以新版 Server 拆分结构为基准解决冲突**

保留上游 `server.go` 的类型、构造流程、启动和关闭逻辑；删除旧文件中已由 `server_management.go`、`server_middleware.go`、`server_routes.go` 承担的重复实现。

- [ ] **步骤 2：恢复管理页面资源接入**

在上游管理路由职责文件中注册并保留：

```go
s.engine.GET(managementasset.RequestLogUsageScriptPath, s.serveRequestLogUsageScript)
s.engine.HEAD(managementasset.RequestLogUsageScriptPath, s.serveRequestLogUsageScript)
s.engine.GET(managementasset.LogQAScriptPath, s.serveLogQAScript)
s.engine.HEAD(managementasset.LogQAScriptPath, s.serveLogQAScript)
```

同时保留 `readManagementHTML` 对 Request Log Usage 与 Log QA 脚本的注入和缓存行为。

- [ ] **步骤 3：恢复管理 API**

在上游管理 API 分组中保留：

```go
mgmt.GET("/request-log-usage", s.mgmt.GetRequestLogUsage)
```

并确认现有 Log QA 路由仍由 Fork 模块注册。

- [ ] **步骤 4：恢复中间件和安全模式白名单**

保持 RequestLoggingMiddleware、按 Key 日志策略，以及 `/v0/management/`、插件资源、`management.html`、Request Log Usage 脚本和 Log QA 脚本在示例 API Key 安全模式下的既有访问行为。

- [ ] **步骤 5：运行 Server 专项测试**

运行：

```bash
go test ./internal/api ./internal/api/handlers/management ./internal/managementasset
```

预期：全部通过。

### 任务 5：迁移请求日志核心二开

**文件：**
- 修改：`internal/logging/request_logger.go`
- 修改：`internal/logging/request_logger_scope.go`（仅在需要承载共享状态时创建）
- 修改：`internal/logging/request_logger_writer.go`
- 修改：`internal/logging/request_logger_streaming.go`
- 修改：`internal/logging/request_logger_format.go`
- 测试：`internal/logging/request_logger_home_test.go`
- 测试：`internal/api/middleware/request_logging_custom_test.go`

- [ ] **步骤 1：采用上游拆分文件作为日志写入基线**

保留上游的 `request_logger_body_source.go`、`request_logger_format.go`、`request_logger_home.go`、`request_logger_streaming.go` 和 `request_logger_writer.go` 职责划分，不把完整旧实现重新塞回 `request_logger.go`。

- [ ] **步骤 2：恢复共享按 Key 配置状态**

确保 `FileRequestLogger` 的作用域副本共享启用状态、Home 状态、错误日志保留数、API Key 名称映射和 no-log 集合。必须保留这些公开行为：

```go
SetAPIKeyNames(keys, names []string)
SetNoLogAPIKeys(keys []string)
ShouldSkipLog(apiKey string) bool
ForAPIKey(apiKey string) RequestLogger
ForKeyLabel(label string) RequestLogger
SanitizeAPIKeyName(name string) string
APIKeyLogDirectory(apiKey string) string
```

- [ ] **步骤 3：恢复原子日志发布**

非流式和流式日志都必须先写同目录临时文件、同步文件、以不覆盖现有日志的方式发布，再同步目录。复用：

```go
publishRequestLog(filePath string, writeLog func(*os.File) error) error
```

流式 `Close()` 必须只在完整内容写入成功后发布最终 `.log` 文件，并清理临时文件。

- [ ] **步骤 4：保持 uploader 所需格式**

验证生成日志继续包含：

```text
=== REQUEST INFO ===
=== HEADERS ===
=== REQUEST BODY ===
=== API REQUEST ===
=== API RESPONSE ===
=== RESPONSE ===
```

不引入旧 hardening 分支的 32 MiB 新限制、鉴权决策上下文或尚未通过测试的压缩请求体捕获行为。

- [ ] **步骤 5：运行日志专项测试**

运行：

```bash
go test ./internal/logging ./internal/api/middleware
```

预期：全部通过，并确认 no-log Key 不产生文件、插件 Key ID 使用正确目录、流式结束后没有 `.tmp` 残留。

### 任务 6：验证 uploader、TOS、Supabase 和统计兼容性

**文件：**
- 验证：`internal/loguploader/`
- 验证：`internal/logqa/`
- 验证：`cmd/log-uploader/`
- 验证：`cmd/log-qa/`

- [ ] **步骤 1：运行日志转换与压缩测试**

运行：

```bash
go test ./internal/loguploader -run 'TestRunOnce|Test.*Archive|Test.*Normalize|Test.*JSONL|Test.*Size' -count=1
```

预期：JSONL 转换、Zstandard 压缩和字节统计测试通过。

- [ ] **步骤 2：运行 TOS 与 Supabase 状态测试**

运行：

```bash
go test ./internal/loguploader -run 'Test.*Upload|Test.*Prepared|Test.*Supabase|Test.*Outbox|Test.*History' -count=1
```

预期：上传幂等、prepared state、Supabase outbox、历史同步和大小字段测试通过。

- [ ] **步骤 3：运行 Log QA 测试**

运行：`go test ./internal/logqa ./internal/api/handlers/management -run 'Test.*LogQA' -count=1`

预期：全部通过。

### 任务 7：格式化、全量验证和提交

**文件：**
- 修改：所有解决冲突后的 Go 文件
- 验证：整个仓库

- [ ] **步骤 1：格式化 Go 代码**

运行：`gofmt -w .`

预期：所有 Go 文件符合 gofmt。

- [ ] **步骤 2：检查残留冲突与差异**

运行：

```bash
rg -n '^(<<<<<<<|=======|>>>>>>>)' --glob '!docs/superpowers/**' .
git diff --check
git status --short
```

预期：无冲突标记和空白错误，所有预期文件已解决。

- [ ] **步骤 3：运行全量测试**

运行：`go test ./...`

预期：全部包通过。

- [ ] **步骤 4：运行强制编译验证**

运行：`go build -o test-output ./cmd/server && rm test-output`

预期：编译成功并删除临时文件。

- [ ] **步骤 5：完成合并提交**

运行：

```bash
git add -A
git commit
```

预期：生成包含上游 `v7.2.133` 第二父提交的 merge commit。

- [ ] **步骤 6：生成冲突报告**

记录真实冲突文件、每个冲突采用的上游/Fork 内容、专项测试、全量测试和编译结果，供发布前复核。
