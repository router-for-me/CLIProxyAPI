### 任务 6：Keeper 解码与落库列

**文件**（cwd `/Users/maverick/cpa-usage-keeper` worktree）：
- 修改：`internal/service/redis_usage.go`（`queuedUsageDetail` + `toUsageEvent`）
- 修改：`internal/entities/usage_event.go`、`internal/entities/usage_event_archive.go`
- 创建：`internal/service/test/redis_usage_upstream_response_model_test.go`
- 创建：`internal/repository/migration/20260917_usage_event_upstream_response_model.go`
- 创建：`internal/repository/migration/test/upstream_response_model_test.go`
- 修改：`internal/repository/migration/migration.go`（常量 + `orderedMigrations` 末尾）

**接口**：
- 消费：队列键 `upstream_response_model`
- 产出：`UsageEvent.UpstreamResponseModel string`（列 `upstream_response_model`，空串默认）
- 覆盖 Scenario：Keeper 持久化、缺省降级（解码侧）
- 来源：spec Requirement「Persist the observed model on request events」

**步骤 1：写失败测试**

仿 `internal/service/test/redis_usage_response_service_tier_test.go`：

```go
func TestDecodeRedisUsageMessageKeepsUpstreamResponseModel(t *testing.T) {
	event, _, err := service.DecodeRedisUsageMessage(`{
		"request_id":"req-up",
		"model":"mapped-model",
		"upstream_response_model":"gpt-5.4",
		"tokens":{}
	}`, time.Date(2026, 9, 17, 1, 0, 0, 0, time.UTC))
	if err != nil { t.Fatal(err) }
	if event.UpstreamResponseModel != "gpt-5.4" {
		t.Fatalf("got %q", event.UpstreamResponseModel)
	}
}

func TestDecodeRedisUsageMessageLeavesMissingUpstreamResponseModelEmpty(t *testing.T) {
	event, _, err := service.DecodeRedisUsageMessage(`{
		"request_id":"req-up-missing","model":"mapped-model","tokens":{}
	}`, time.Date(2026, 9, 17, 1, 0, 0, 0, time.UTC))
	if err != nil { t.Fatal(err) }
	if event.UpstreamResponseModel != "" {
		t.Fatalf("got %q", event.UpstreamResponseModel)
	}
}
```

迁移测仿 `test/response_service_tier_test.go` 的 MarkAllAsApplied + 撤回版本：建仅含 `id` 的旧表，插入一行，跑 `migration.Run`，断言 `HasColumn("usage_events", "upstream_response_model")` 且该行值为 `""`。**不要**回填 service_tier。版本字符串：`20260917_add_usage_event_upstream_response_model`。

**步骤 2：运行测试确认失败**

运行：`go test ./internal/service/test/ -run 'TestDecodeRedisUsageMessageKeepsUpstreamResponseModel|TestDecodeRedisUsageMessageLeavesMissingUpstreamResponseModelEmpty' -count=1`

预期：字段不存在导致编译失败，或值为空导致 AssertionError。导入失败不算有效红。

**步骤 3：写最小实现**

`queuedUsageDetail`：`UpstreamResponseModel string \`json:"upstream_response_model"\``。`toUsageEvent`：`UpstreamResponseModel: strings.TrimSpace(d.UpstreamResponseModel)`。

实体（热表与归档）在 `ModelAlias` 旁：

```go
UpstreamResponseModel string `gorm:"column:upstream_response_model;not null;default:''"`
```

`UsageEventStorageColumns` 在 `model_alias` 后插入 `upstream_response_model`（归档复制契约，漏了会红 `TestUsageEventArchiveSchemaMatchesHotColumnsWithoutSecondaryIndexes`）。

迁移：热表 `AddColumn`；若 `HasTable` 归档则同样加列。`migration.go` 登记常量并挂到 `orderedMigrations` **最后一项**。

**步骤 4：运行测试确认通过**

运行：`go test ./internal/service/test/ ./internal/repository/migration/ ./internal/repository/test/ -run 'UpstreamResponseModel|UsageEventArchiveSchema' -count=1`

预期：PASS。`gofmt -w` 本票 Go 文件。

**步骤 5：提交**

```bash
git add internal/service/redis_usage.go internal/service/test/redis_usage_upstream_response_model_test.go internal/entities/usage_event.go internal/entities/usage_event_archive.go internal/repository/migration/
git commit -m "feat(T06): persist upstream_response_model on usage events"
```
