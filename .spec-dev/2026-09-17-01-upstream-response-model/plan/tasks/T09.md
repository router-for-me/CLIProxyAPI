### 任务 9：验收（acceptance-qa）

> 本任务由 executing-plans 收尾审查阶段触发 acceptance-qa 按下表执行，
> 不参与逐任务连续执行；报告与证据落盘特性目录 `acceptance/` 子目录。

| Scenario / 检查项 | 维度 | 执行方式 | 目标 | 阈值/预期 | 验收证据 |
|-------------------|------|---------|------|----------|---------|
| CPA 入队后 Keeper 能展示 mismatch 行 | e2e | 验收任务 (D) | Usage 请求事件表 + 凭证详情请求表 | 发往上游 `model` 与观测值不一致时，两处模型列出现第三行 `上游响应: {name}`（或英文 `Upstream response: {name}`）；一致/空值不出现；CSV 表头含 `upstream_response_model` | `acceptance/` 下截图或联调记录；相关单测已绿 |

**文件**：不改生产代码，除非前序票漏测。

**接口**：
- 消费：T01–T08 全部产出
- 产出：对照 spec Success Criteria 的验收记录
- 覆盖 Scenario：上表 e2e 行；其余 Scenario 已由 T01–T08 任务内 TDD 覆盖
- 来源：spec 验收矩阵最后一行

手工联调最小路径（本地已有 CPA + Keeper）：

1. 发一条 force-mapping 会改写出站模型、但上游原文 `model` 不同的请求。
2. 确认入队 JSON 含 `upstream_response_model`。
3. 打开 Usage「请求事件」与凭证详情请求表，核对第三行只在 mismatch 出现，无徽章。
4. 导出 CSV，表头在 `model_alias` 后含 `upstream_response_model`。

回归命令（CPA worktree）：

```bash
gofmt -l sdk/cliproxy/usage internal/runtime/executor/helps internal/redisqueue sdk/cliproxy/auth/conductor_execution.go
go test ./sdk/cliproxy/usage/ ./internal/runtime/executor/helps/ ./internal/redisqueue/ -count=1
go build -o test-output ./cmd/server && rm test-output
```

Keeper worktree：

```bash
gofmt -l internal/service internal/entities internal/repository internal/api
go test ./internal/service/ ./internal/service/test/ ./internal/repository/ ./internal/repository/migration/ ./internal/api/test/ -count=1
pnpm --dir web exec vitest run src/components/usage/test/RequestEventsDetailsCardModelAlias.test.tsx src/components/usage/credentials/test/CredentialRequestEventsList.test.tsx src/i18n/index.test.ts
```

预期：全部 PASS。缺口回到对应 T0x，不在本票加需求。
