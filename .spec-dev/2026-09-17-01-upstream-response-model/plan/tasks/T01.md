### 任务 1：usage 观察器与 Record 字段

**文件**：
- 创建：`sdk/cliproxy/usage/upstream_response_model.go`
- 创建：`sdk/cliproxy/usage/upstream_response_model_test.go`
- 修改：`sdk/cliproxy/usage/manager.go`（`Record` 增加字段）

**接口**：
- 消费：无
- 产出：`BeginUpstreamResponseModelObservation(ctx context.Context) context.Context`；`ObserveUpstreamResponseModel(ctx context.Context, model string, terminal bool)`；`RememberUpstreamResponseEventType(ctx context.Context, eventType string)`；`LastUpstreamResponseEventType(ctx context.Context) string`；`UpstreamResponseModelFromContext(ctx context.Context) string`；`BindUpstreamResponseModelFamily(ctx context.Context, family ExtractionFamily)`；`MapExecutorToExtractionFamily(provider, executorType string) ExtractionFamily`；`type ExtractionFamily string`（`openai`/`claude`/`gemini`）；`Record.UpstreamResponseModel string`
- 覆盖 Scenario：无声明则保持空、超长名称截断、重试不串味；依赖替换：无
- 来源：spec Requirement「Isolate observation per upstream attempt」「Extract models…」截断/空值规则

**步骤 1：写失败测试**

在 `sdk/cliproxy/usage/upstream_response_model_test.go`：

```go
func TestUpstreamResponseModelFromContextEmpty(t *testing.T) {
	ctx := BeginUpstreamResponseModelObservation(context.Background())
	if got := UpstreamResponseModelFromContext(ctx); got != "" {
		t.Fatalf("got %q, want empty", got)
	}
}

func TestObserveUpstreamResponseModelTruncatesRunes(t *testing.T) {
	ctx := BeginUpstreamResponseModelObservation(context.Background())
	name := strings.Repeat("模", 201)
	ObserveUpstreamResponseModel(ctx, "  "+name+"  ", true)
	got := []rune(UpstreamResponseModelFromContext(ctx))
	if len(got) != 200 {
		t.Fatalf("len=%d, want 200", len(got))
	}
}

func TestBeginReplacesObserver(t *testing.T) {
	ctx := BeginUpstreamResponseModelObservation(context.Background())
	ObserveUpstreamResponseModel(ctx, "model-a", true)
	ctx = BeginUpstreamResponseModelObservation(ctx)
	ObserveUpstreamResponseModel(ctx, "model-b", true)
	if got := UpstreamResponseModelFromContext(ctx); got != "model-b" {
		t.Fatalf("got %q, want model-b", got)
	}
}

func TestMapExecutorToExtractionFamily(t *testing.T) {
	if got := MapExecutorToExtractionFamily("claude", "ClaudeExecutor"); got != FamilyClaude {
		t.Fatalf("got %q, want claude", got)
	}
	if got := MapExecutorToExtractionFamily("antigravity", "AntigravityExecutor"); got != FamilyGemini {
		t.Fatalf("got %q, want gemini", got)
	}
	if got := MapExecutorToExtractionFamily("openai", "CodexExecutor"); got != FamilyOpenAI {
		t.Fatalf("got %q, want openai", got)
	}
}
```

**步骤 2：运行测试确认失败**

运行：`go test ./sdk/cliproxy/usage/ -run 'TestUpstreamResponseModelFromContextEmpty|TestObserveUpstreamResponseModelTruncatesRunes|TestBeginReplacesObserver|TestMapExecutorToExtractionFamily' -count=1`

预期：编译失败或 FAIL，断言未实现。导入/零测试不算有效红。

**步骤 3：写最小实现**

`Record` 增加 `UpstreamResponseModel string`（放在 `Alias` 旁）。新文件：ctx 上放 `*observer`（first/terminal/eventType/family）。`Observe`：trim、空返回、rune 截 200、terminal 覆盖、非 terminal 只写 first。`Begin` 换新 observer。`MapExecutorToExtractionFamily`：标识含 `claude` → Claude；含 `gemini`/`antigravity`/`aistudio`/`vertex` → Gemini；否则 OpenAI。大小写不敏感。

**步骤 4：运行测试确认通过**

运行：同步骤 2。预期：PASS。然后 `gofmt -w sdk/cliproxy/usage/`。

**步骤 5：提交**

```bash
git add sdk/cliproxy/usage/upstream_response_model.go sdk/cliproxy/usage/upstream_response_model_test.go sdk/cliproxy/usage/manager.go
git commit -m "feat(T01): add upstream response model observer"
```
