### 任务 8：请求事件模型列第三行

**文件**（Keeper worktree）：
- 修改：`web/src/lib/types.ts`（`UsageEvent`）
- 修改：`web/src/i18n/index.ts`（en / zh / zh-TW）
- 修改：`web/src/i18n/index.test.ts`（若该文件锁列标签）
- 修改：`web/src/components/usage/RequestEventsDetailsCard.tsx`
- 修改：`web/src/components/usage/credentials/CredentialRequestEventsList.tsx`
- 测试：`web/src/components/usage/test/RequestEventsDetailsCardModelAlias.test.tsx`
- 测试：`web/src/components/usage/credentials/test/CredentialRequestEventsList.test.tsx`

**接口**：
- 消费：列表字段 `upstream_response_model`
- 产出：mismatch 时第三行 `{t('usage_stats.upstream_response_model')}: {name}`
- 覆盖 Scenario：不一致才显示第三行、一致或空值不显示
- 来源：spec Requirement「Show the upstream response model only on mismatch」

**步骤 1：写失败测试**

`RequestEventsDetailsCardModelAlias.test.tsx` 增加事件 `upstream_response_model: 'actual-model'`（model 仍为 `claude-sonnet`）。英文 locale 下模型单元格须含 `Upstream response: actual-model`，且不含徽章文案。再测 `upstream_response_model` 等于 model（仅大小写不同）或空时，单元格不含 `Upstream response`。

凭证列表测试同样：`model`/`upstream_response_model` 不一致时 `textContent` 含 `Upstream response`。

**步骤 2：运行测试确认失败**

运行：`pnpm --dir web exec vitest run src/components/usage/test/RequestEventsDetailsCardModelAlias.test.tsx src/components/usage/credentials/test/CredentialRequestEventsList.test.tsx`

预期：找不到 `Upstream response` 子串。

**步骤 3：写最小实现**

i18n 三语：`upstream_response_model: 'Upstream response' | '上游响应' | '上游響應'`。

行映射：

```ts
const upstream = String(event.upstream_response_model ?? '').trim()
const showUpstream = Boolean(upstream && upstream.toLowerCase() !== modelValue.toLowerCase())
```

`RequestEventsDetailsCard.tsx` 模型单元格（约 857–858 行）在 alias `span` 后：`showUpstream` 时再渲染一行 `{t('usage_stats.upstream_response_model')}: {upstream}`。不要新列 ID，不升 preferences version。

`CredentialRequestEventsList.tsx` 模型单元格（约 655–657 行）：`strong` model、`small` alias 之后、`reasoning_effort` 行之前，条件渲染同一文案。行映射增加 `upstreamResponseModel` 与 `showUpstream`。

`types.ts`：`upstream_response_model?: string`。

**步骤 4：运行测试确认通过**

运行：同步骤 2 以及 `pnpm --dir web exec vitest run src/i18n/index.test.ts`。预期：PASS。

**步骤 5：提交**

```bash
git add web/src/lib/types.ts web/src/i18n/index.ts web/src/i18n/index.test.ts web/src/components/usage/RequestEventsDetailsCard.tsx web/src/components/usage/credentials/CredentialRequestEventsList.tsx web/src/components/usage/test/RequestEventsDetailsCardModelAlias.test.tsx web/src/components/usage/credentials/test/CredentialRequestEventsList.test.tsx
git commit -m "feat(T08): show mismatched upstream response model"
```
