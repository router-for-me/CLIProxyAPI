### 任务 10：合并与清理

**资源台账**（`plan/progress.yaml` 的 `resources`）：只处理本计划拥有的两条 worktree；台账外不动；共享缓存默认保留。T00 核实后把预登记路径改成实际路径/分支/归属。复用隔离只写保留/移交，不预填删除。

**文件**：
- CPA 来源仓：`/Users/maverick/CLIProxyAPI`（路径以 T00 notes 为准）
- Keeper 来源仓：`/Users/maverick/cpa-usage-keeper`
- spec：`.spec-dev/2026-09-17-01-upstream-response-model/spec/upstream-response-model-design.md`

**接口**：
- 消费：T00 notes 中的来源/实施工作区与分支；T09 验收通过
- 产出：两仓实施分支已合入各自来源分支；`sync_commit` 锚定；`current: null`
- 覆盖 Scenario：无新增行为
- 来源：spec Hard Constraints「同一份计划、CPA 先合再 Keeper」

**步骤 1：全量验证（安全网）与归属裁决**

确认 T00–T09 均为 completed，本票先标 `in_progress`。在各实施 worktree 跑完整套件：

```bash
# CPA
go test ./...
go build -o test-output ./cmd/server && rm test-output
# Keeper
go test ./...
pnpm --dir web exec vitest run
```

全绿 → 步骤 2。范围内失败 → 修复后重跑。范围外失败 → 在来源工作区同源分支复跑：同源也失败则报告既有失败请用户裁决；同源通过则当本次回归修复。

**步骤 2：测试退役检查**

扫描本计划相关测试范围。孤儿定义：测试名对不上任何 active spec 现行 Scenario，且对应 Requirement 已 REMOVED 或带 `Superseded`。本特性无 REMOVED/Superseded → 声明「无孤儿测试」并跳过。

**步骤 3：取代回写**

本 spec `supersedes: []` → 声明「无取代回写」并跳过。

**步骤 4：合并回来源分支**

从 T00 notes 读实际来源工作区、来源分支、实施工作区、实施分支。先在实施工作区确认干净，再在来源工作区核对当前分支干净，然后：

```bash
git -C <CPA来源工作区> merge <CPA实施分支>
git -C <Keeper来源工作区> merge <Keeper实施分支>
```

顺序固定：先 CPA 后 Keeper。不硬编码分支名、不用别票 shell 变量。冲突或来源工作区不干净 → 停下确认。本任务默认不 `gh pr create`、不 push，除非用户本轮明确要求。复用隔离则移交回原机制，notes 记「已移交」而非「已合并」。

**步骤 5：清理（按资源台账逐条执行）**

从已合并的 CPA 来源工作区读 progress。对本计划拥有的 worktree 执行台账清理命令（`git worktree remove`，勿 `--force` 除非确认干净）。成功移除、失败保留并记 blocked、已不存在核实后移除。notes 追加真实结果，一次提交：

```bash
git add .spec-dev/2026-09-17-01-upstream-response-model/plan/progress.yaml
git commit -m "chore(T10): cleanup ledger"
```

不能在已删 worktree 里继续写文件。

**步骤 6：sync_commit 锚定**

在已取得真实合并证据的 CPA 来源工作区：

```bash
SYNC=$(git rev-parse HEAD)
# 更新 spec frontmatter sync_commit 为 $SYNC，保留其余键
git add .spec-dev/2026-09-17-01-upstream-response-model/spec/upstream-response-model-design.md
git commit -m "chore(spec): sync_commit 锚定 ${SYNC:0:7}"
```

尚未从原隔离机制取得合并回执时保持 in_progress，不提前锚定。

**步骤 7：最终状态保存**

原子写入：T10 `status: completed`、`current: null`、`commit` 为实际验证 SHA。独立提交。合并/清理/锚定未完成则保持等待/blocked，不写 completed。
