### 任务 0：建立隔离工作区

**文件**：
- 不改生产代码
- 状态：`.spec-dev/2026-09-17-01-upstream-response-model/plan/progress.yaml`

**接口**：
- 消费：无
- 产出：CPA 实施工作区与分支；Keeper 实施工作区与分支（记录在 progress notes）

**步骤 1：检测已有隔离**

在 `/Users/maverick/CLIProxyAPI` 运行：`git rev-parse --git-dir` 与 `git rev-parse --git-common-dir`。两者不同、且 `git rev-parse --show-superproject-working-tree` 无输出 → 已在隔离工作区，跳过步骤 2 的 CPA 创建，仍做绑定、步骤 3 与完成状态保存。

对 `/Users/maverick/cpa-usage-keeper` 做同样检测。

创建前记录：来源工作区绝对路径、来源分支、HEAD。复用时核对原记录，不能假定来源为 main。用 progress `notes` 保存 created/inherited 与移交方式。

**步骤 2：建立 worktree**

CPA：确认 `git check-ignore -q .worktrees`（未忽略则加入 `.gitignore` 并提交），然后

```bash
git -C /Users/maverick/CLIProxyAPI worktree add .worktrees/plan/2026-09-17-01-upstream-response-model -b plan/2026-09-17-01-upstream-response-model
```

Keeper：

```bash
git -C /Users/maverick/cpa-usage-keeper worktree add .worktrees/plan/2026-09-17-01-upstream-response-model -b plan/2026-09-17-01-upstream-response-model
```

后续 CPA 任务在 CPA worktree 执行；T06–T08 在 Keeper worktree 执行。有 using-git-worktrees / EnterWorktree 时优先用原生工具，命令等价即可。

**步骤 3：安装依赖并验证基线**

CPA：已有 Go 模块则不重装。运行：

```bash
go test ./sdk/cliproxy/usage/ ./internal/runtime/executor/helps/ ./internal/redisqueue/
```

预期：PASS（此时尚无本特性新测试文件）。失败 → 停下报告。

Keeper：Go 模块已就绪则不重装。运行：

```bash
go test ./internal/service/ ./internal/api/test/ ./internal/repository/
```

预期：PASS。前端基线：

```bash
pnpm --dir web exec vitest run src/components/usage/test/RequestEventsDetailsCardModelAlias.test.tsx
```

预期：PASS。

将实际基线 SHA 写入 T00.commit，status=completed，`current: T01`。状态写入用同目录临时文件 + rename，保留原 tasks/notes/resources。在 CPA 来源仓单独提交 progress（该 SHA 是基线提交，不是本条 commit）：

```bash
git -C /Users/maverick/CLIProxyAPI add .spec-dev/2026-09-17-01-upstream-response-model/plan/progress.yaml
git -C /Users/maverick/CLIProxyAPI commit -m "chore(T00): record isolated workspaces"
```

notes 追加：两仓来源路径/分支/HEAD、实施路径/分支、created 或 inherited。复用隔离时 resources 改写为保留/移交，不预填删除。
