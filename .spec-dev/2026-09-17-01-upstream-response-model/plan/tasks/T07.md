### 任务 7：Keeper 列表与导出

**文件**（Keeper worktree）：
- 修改：`internal/repository/usage.go`（`usageEventProjectionColumns`、投影 struct、`ToRecord`）
- 修改：`internal/repository/query_projection_test.go`（字面量快照）
- 修改：`internal/repository/dto/usage_events.go`、`internal/service/dto/usage.go`、`internal/service/usage.go`（List/Stream 拷贝）
- 修改：`internal/api/usage_events.go`（payload、CSV header/record）
- 测试：`internal/api/test/usage_events_test.go`、`internal/service/test/usage_events_model_alias_test.go`

**接口**：
- 消费：`UsageEvent.UpstreamResponseModel`
- 产出：列表 JSON `upstream_response_model`；CSV/JSON 导出同名字段
- 覆盖 Scenario：导出始终带列、Keeper 持久化（读路径）
- 来源：spec Requirement「Include the observed model in event exports」

**步骤 1：写失败测试**

`usage_events_test.go` 列表响应断言 `upstream_response_model`。CSV 测试在现有 header 断言里、`model_alias` 之后插入 `upstream_response_model`（先读现行 `usageEventsExportCSVHeader` 顺序，插在 `model_alias` 后）。插入带该字段的事件，断言单元格值。

`query_projection_test.go`：把 `usageEventProjectionColumns` 的期望字面量补上 `upstream_response_model`（改 SELECT 必改此测）。

**步骤 2：运行测试确认失败**

运行：`go test ./internal/api/test/ ./internal/repository/ -run 'UsageEvent|query_projection|Query' -count=1`

预期：header / JSON 缺字段 FAIL。

**步骤 3：写最小实现**

`usageEventProjectionColumns` 在 `model_alias` 后加 `upstream_response_model`。**不要**改 `usageOverviewBoundaryEventProjectionColumns` / `usageOverviewRealtimeEventProjectionColumns`（概览/分析不加该维度）。投影 struct 与 DTO 加 `UpstreamResponseModel string`。`service/usage.go` List/Stream 手工拷贝该字段（约 724 与 779 两处，以现行文件为准）。

`usageEventPayload`：`UpstreamResponseModel string \`json:"upstream_response_model,omitempty"\``。CSV header 在 `model_alias` 后加列，record 写 `row.UpstreamResponseModel`。JSON 导出同样。

**步骤 4：运行测试确认通过**

运行：`go test ./internal/api/test/ ./internal/service/test/ ./internal/repository/ -count=1`。预期：PASS。

**步骤 5：提交**

```bash
git add internal/repository/usage.go internal/repository/query_projection_test.go internal/repository/dto internal/service/dto/usage.go internal/service/usage.go internal/api/usage_events.go internal/api/test/usage_events_test.go
git commit -m "feat(T07): expose upstream_response_model on events API"
```
