{
  "id": "packycode-codex-toggle-20251028",
  "title": "Proposal: Packycode ↔ Codex 运行时互斥开关",
  "deltaCount": 2,
  "deltas": [
    {
      "spec": "runtime-toggle",
      "operation": "ADDED",
      "description": "Add requirement: 系统 SHALL 在 `packycode.enabled=true` 时禁用 `provider=codex` 的认证与路由，并在 `false` 时仅恢复因该开关禁用的 Codex 认证。"
    },
    {
      "spec": "runtime-toggle",
      "operation": "ADDED",
      "description": "Add requirement: 系统 SHALL 在执行互斥切换后，更新模型注册表以反映 `/v1/models` 的可见性：启用 Packycode 时仅暴露 `provider=packycode` 的 OpenAI(GPT) 集合；关闭时恢复 Codex 集合。"
    }
  ]
}

